# Miscellaneous

---

## Latex tricks

* Format is off on only some page numbers
  * Figure messes up the environment.
    * Fix:
      Change
      ```
       \begin{figure}
       ... \textwidth
      ```
      to
      ```
       \begin{figure}
       ... \columnwidth      
      ```

---
